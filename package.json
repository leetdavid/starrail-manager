{
  "name": "starrail-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch:api": "cd api && pnpm dev",
    "watch:app": "cd app && pnpm dev",
    "dev": "concurrently -c \"auto\" \"pnpm:watch:*\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd api && pnpm install && cd ../app && pnpm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
